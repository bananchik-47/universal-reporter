🚀 Установка и запуск Universal Reporter
 1. Склонируй или скачай проект

Если у тебя есть Git:

git clone https://github.com/твоя-ссылка-на-репозиторий.git
cd universal-reporter


Если нет — просто скачай ZIP-архив проекта, распакуй и открой папку universal-reporter в терминале.

 2. Создай и активируй виртуальное окружение

Windows (cmd или PowerShell):

python -m venv .venv
.venv\Scripts\activate


Linux / macOS:

python3 -m venv .venv
source .venv/bin/activate

 3. Установи зависимости
pip install -r requirements.txt

 4. Запусти программу

Пример запуска:

python main.py examples/sonar-report.json

 5. Результат работы

После выполнения появится:

report.html — готовый HTML-отчёт

report.xlsx — отчёт в Excel

report.pdf — отчёт в PDF

Все файлы сохраняются в корень проекта.
